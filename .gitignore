# Build directories
build/
build-asan/
build-tsan/
out/

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
compile_commands.json

# VSCode (keep tasks/launch/extensions, ignore local settings)
.vscode/settings.json

# macOS
.DS_Store

# Devcontainer: host-local zshrc (generated by init-zshrc.sh, not for committing)
.devcontainer/.zshrc.local

# Fetched dependencies (FetchContent downloads to build dir, but just in case)
_deps/

# clangd index cache
.cache/
