# Bazel output symlinks
/bazel-*

# VSCode (keep tasks/launch/extensions, ignore local settings)
.vscode/settings.json

# macOS
.DS_Store

# Devcontainer: host-local zshrc (generated by init-zshrc.sh, not for committing)
.devcontainer/.zshrc.local

# clangd index cache
.cache/

# compile_commands.json (generated by hedron_compile_commands or similar)
compile_commands.json
